/**
 * Fidelidade — apenas DIRECTOR/ADMIN (per CLAUDE.md). Sprint 4 FIX 6.
 */
export default function FidelidadePage() {
  return (
    <div data-testid="fidelidade-page" style={{ padding: '2rem' }}>
      <h1>Programa Fidelidade</h1>
      <p>Gerencie pontos e recompensas (DIRETOR/ADMIN only)</p>
    </div>
  );
}
