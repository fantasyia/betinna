# =====================================================================
# Betinna.ai — Variáveis de ambiente Railway (production)
# Sprint 4 FIX 2
#
# Como usar:
#   1. Cole cada var abaixo no dashboard Railway de cada service
#   2. Marque ENCRYPTION_KEY e secrets como "sensíveis" no Railway
#   3. Railway injeta DATABASE_URL e REDIS_URL automaticamente quando você
#      adiciona os plugins PostgreSQL e Redis
#   4. PORT é injetado automaticamente pelo Railway — NÃO defina manualmente
#
# Estrutura de Railway services:
#   - api      (Web service): startCommand = "npm run start:prod"
#   - worker   (Worker):      startCommand = "npm run worker"
#   - frontend (Static site): build = "npm run build", output = "dist/"
# =====================================================================

# ─── App (obrigatórios) ─────────────────────────────────────────────────
NODE_ENV=production
API_PREFIX=api/v1
LOG_LEVEL=info
# PORT — INJETADO PELO RAILWAY. Não defina manualmente.

# ─── Service identity (defina no service Railway correspondente) ─────────
# API service:
SERVICE_TYPE=api
# Worker service:
# SERVICE_TYPE=worker

# Railway injeta automaticamente — usado para TLS Redis e cron lock
# RAILWAY_ENVIRONMENT=production
# INSTANCE_ID=$RAILWAY_REPLICA_ID  (ou deixe vazio; fallback é host-<pid>)

# ─── CORS (configure com domínio frontend Railway) ───────────────────────
# Comma-separated. NUNCA use "*" em produção.
CORS_ORIGINS=https://betinna.up.railway.app,https://betinna.ai

# ─── Database — INJETADO PELO RAILWAY POSTGRES PLUGIN ───────────────────
# Plugin adiciona automaticamente:
#   DATABASE_URL=postgresql://user:pass@host:port/db
# Para Prisma migrate (direct connection sem pooler):
#   DIRECT_URL=postgresql://user:pass@host:port/db
# Em Supabase, copiar Settings → Database → Connection string

# ─── Redis — INJETADO PELO RAILWAY REDIS PLUGIN ─────────────────────────
# Plugin adiciona automaticamente:
#   REDIS_URL=redis://default:password@host:port  (ou rediss:// em prod com TLS)

# ─── Supabase Auth + Storage (obrigatórios) ─────────────────────────────
SUPABASE_URL=https://[ref].supabase.co
SUPABASE_ANON_KEY=eyJhbGciOi...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...
# Settings → API → JWT Settings — preencher pra HS256 verify mais rápido
SUPABASE_JWT_SECRET=

# ─── Crypto AES-256 (obrigatório) ────────────────────────────────────────
# Gerar com: openssl rand -hex 32
# NUNCA reusar a chave de staging em produção.
ENCRYPTION_KEY=

# ─── Auth Cache ──────────────────────────────────────────────────────────
AUTH_CACHE_TTL_SECONDS=60

# ─── OMIE (ERP) — webhook obrigatório em prod ────────────────────────────
OMIE_APP_KEY=
OMIE_APP_SECRET=
OMIE_WEBHOOK_SECRET=     # OBRIGATÓRIO em production
OMIE_DEMO_MODE=false
OMIE_BASE_URL=https://app.omie.com.br/api/v1
OMIE_TIMEOUT_MS=30000

# ─── Mercado Livre ──────────────────────────────────────────────────────
ML_CLIENT_ID=
ML_CLIENT_SECRET=
ML_REDIRECT_URI=https://betinna.up.railway.app/api/v1/integracoes/mercadolivre/oauth/callback
# OBRIGATÓRIO em production — IPs do ML (ML não tem HMAC)
ML_WEBHOOK_IP_WHITELIST=54.88.218.97,18.215.140.160,18.213.114.129,18.206.34.84
ML_SITE_ID=MLB

# ─── Shopee — partner key obrigatória em prod ───────────────────────────
SHOPEE_PARTNER_ID=
SHOPEE_PARTNER_KEY=      # OBRIGATÓRIO em production
SHOPEE_REDIRECT_URI=https://betinna.up.railway.app/api/v1/integracoes/shopee/oauth/callback
SHOPEE_ENV=live

# ─── Amazon SP-API ───────────────────────────────────────────────────────
AMAZON_CLIENT_ID=
AMAZON_CLIENT_SECRET=
AMAZON_REFRESH_TOKEN=
AMAZON_APP_ID=
AMAZON_LWA_REDIRECT_URI=https://betinna.up.railway.app/api/v1/integracoes/amazon/oauth/callback
AMAZON_MARKETPLACE_ID=A2Q3Y263D00KWC
AMAZON_SP_API_REGION=NA
AMAZON_OAUTH_HOST=sellercentral.amazon.com.br
AMAZON_SANDBOX=false

# ─── TikTok Shop — app secret obrigatório em prod ────────────────────────
TIKTOK_APP_KEY=
TIKTOK_APP_SECRET=       # OBRIGATÓRIO em production
TIKTOK_SERVICE_ID=
TIKTOK_REDIRECT_URI=https://betinna.up.railway.app/api/v1/integracoes/tiktok/oauth/callback
TIKTOK_API_VERSION=202309

# ─── Meta (Facebook + Instagram) — secrets obrigatórios em prod ─────────
META_GRAPH_APP_ID=
META_GRAPH_APP_SECRET=    # OBRIGATÓRIO em production
META_GRAPH_VERIFY_TOKEN=  # OBRIGATÓRIO em production (handshake GET)
META_GRAPH_REDIRECT_URI=https://betinna.up.railway.app/api/v1/integracoes/meta/oauth/callback
META_GRAPH_API_VERSION=v21.0

# ─── Google Calendar ────────────────────────────────────────────────────
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://betinna.up.railway.app/api/v1/integracoes/google/oauth/callback

# ─── WhatsApp Cloud (NÃO Baileys — Baileys roda sem credenciais externas) ───
# Mantido pra compatibilidade futura. Em prod Betinna.ai usa Baileys via Postgres.
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_BUSINESS_ACCOUNT_ID=
WHATSAPP_VERIFY_TOKEN=
WHATSAPP_APP_SECRET=

# ─── IA (MullerBot + Campanhas IA) ──────────────────────────────────────
# Ambiente compartilhado quando user não configurou própria chave
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
MULLERBOT_MODEL=gpt-4o-mini
MULLERBOT_MAX_INPUT_TOKENS=4000
MULLERBOT_MAX_OUTPUT_TOKENS=1024

# ─── E-mail transacional ────────────────────────────────────────────────
SENDGRID_API_KEY=
SENDGRID_FROM_EMAIL=noreply@betinna.ai
SENDGRID_FROM_NAME=Betinna.ai

# ─── Observability ──────────────────────────────────────────────────────
# Projeto Sentry com beforeSend strip PII configurado
# Vazio = Sentry desligado (modo no-op).
SENTRY_DSN=

# =====================================================================
# Variáveis E2E (para CI/CD GitHub Actions, opcional)
# Estas NÃO vão no Railway production — apenas no GitHub Actions secrets
# =====================================================================
# E2E_BASE_URL=https://betinna.up.railway.app
# E2E_TEST_EMAIL=admin@betinna.ai
# E2E_TEST_PASSWORD=
# E2E_TEST_EMAIL_REP=rep@betinna.ai
# E2E_TEST_EMAIL_GERENTE=gerente@betinna.ai
# E2E_TEST_EMAIL_DIRETOR=diretor@betinna.ai
# E2E_TEST_EMAIL_ADMIN=admin@betinna.ai

# =====================================================================
# Frontend (Railway service separado — root /frontend)
# Apenas VITE_* são bundled no JS. NUNCA coloque secrets aqui.
# =====================================================================
# VITE_API_URL=https://betinna-api.up.railway.app
# VITE_SUPABASE_URL=https://[ref].supabase.co
# VITE_SUPABASE_ANON_KEY=eyJhbGciOi...
# VITE_SENTRY_DSN=
